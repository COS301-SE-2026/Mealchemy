from src.models.recommendation import RecommendationItem

# Stub function to be implemented later after more population happens
def dedup(recommendations: list[RecommendationItem]) -> list[RecommendationItem]:
    return recommendations