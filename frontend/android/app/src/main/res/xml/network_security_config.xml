<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security policy for the Mealchemy Android app.

  Production: cleartext (http://) traffic is disallowed everywhere by default.
  Dev only:
  cleartext is permitted for the Android emulator's loopback
  (10.0.2.2 = the host machine from inside the emulator) and
  for localhost, so the app can reach a local Spring Boot
  backend during development.

  When the backend moves to HTTPS in staging/production, those domains
  inherit base-config and require TLS automatically.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false"/>
</network-security-config>
